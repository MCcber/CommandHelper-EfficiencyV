<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Input="clr-namespace:CBHK.CustomControl.Input"
    xmlns:VectorButton="clr-namespace:CBHK.CustomControl.VectorButton"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors">

    <Style x:Key="VectorNumbericUpDownStyle" TargetType="{x:Type Input:VectorNumbericUpDown}">
        <Style.Setters>
            <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Input:VectorNumbericUpDown}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition MinHeight="40" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Input:VectorTextBox
                                Grid.Column="0"
                                Padding="2,0,0,0"
                                HorizontalContentAlignment="Left"
                                VerticalContentAlignment="Center"
                                Background="#313233"
                                BorderBrush="#242425"
                                Foreground="{DynamicResource CommonForeground}"
                                LocateLineBrush="{TemplateBinding LocateLineBrush}"
                                Style="{DynamicResource VectorTextBoxStyle}"
                                Text="{Binding Text, RelativeSource={RelativeSource Mode=TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"
                                WaterMarkerBrush="{TemplateBinding WaterMarkerBrush}"
                                WaterMarkerMargin="{TemplateBinding WaterMarkerMargin}"
                                WaterMarkerText="{TemplateBinding WaterMarkerText}">
                                <behaviors:Interaction.Triggers>
                                    <behaviors:EventTrigger EventName="Loaded">
                                        <behaviors:CallMethodAction MethodName="VectorNumbericUpDownTextBox_Loaded" TargetObject="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                    </behaviors:EventTrigger>
                                    <behaviors:EventTrigger EventName="TextChanged">
                                        <behaviors:CallMethodAction MethodName="OnTextChanged" TargetObject="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                    </behaviors:EventTrigger>
                                    <behaviors:EventTrigger EventName="GotFocus">
                                        <behaviors:CallMethodAction MethodName="VectorNumbericUpDown_GotFocus" TargetObject="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                    </behaviors:EventTrigger>
                                    <behaviors:EventTrigger EventName="LostFocus">
                                        <behaviors:CallMethodAction MethodName="VectorNumbericUpDown_LostFocus" TargetObject="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                    </behaviors:EventTrigger>
                                </behaviors:Interaction.Triggers>
                            </Input:VectorTextBox>
                            <VectorButton:VectorIconRepeatButton
                                Grid.Column="1"
                                Background="{TemplateBinding Background}"
                                IconAngle="90"
                                IconBrush="{DynamicResource CommonForeground}"
                                OriginBottomHeight="3"
                                Style="{DynamicResource VectorIconRepeatButtonStyle}">
                                <behaviors:Interaction.Triggers>
                                    <behaviors:EventTrigger EventName="Click">
                                        <behaviors:CallMethodAction MethodName="OnDecrease_Click" TargetObject="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                    </behaviors:EventTrigger>
                                </behaviors:Interaction.Triggers>
                            </VectorButton:VectorIconRepeatButton>
                            <VectorButton:VectorIconRepeatButton
                                Grid.Column="2"
                                Background="{TemplateBinding Background}"
                                IconAngle="-90"
                                IconBrush="{DynamicResource CommonForeground}"
                                OriginBottomHeight="3"
                                Style="{DynamicResource VectorIconRepeatButtonStyle}">
                                <behaviors:Interaction.Triggers>
                                    <behaviors:EventTrigger EventName="Click">
                                        <behaviors:CallMethodAction MethodName="OnIncrease_Click" TargetObject="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                    </behaviors:EventTrigger>
                                </behaviors:Interaction.Triggers>
                            </VectorButton:VectorIconRepeatButton>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
</ResourceDictionary>