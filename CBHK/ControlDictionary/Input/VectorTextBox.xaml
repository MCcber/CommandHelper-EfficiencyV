<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Container="clr-namespace:CBHK.CustomControl.Container"
    xmlns:Input="clr-namespace:CBHK.CustomControl.Input">

    <Style x:Key="VectorTextBoxStyle" TargetType="{x:Type Input:VectorTextBox}">
        <Setter Property="AllowDrop" Value="true" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
        <Setter Property="WaterMarkerMargin" Value="5,0,0,0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="CaretBrush" Value="White" />
        <Setter Property="RenderOptions.BitmapScalingMode" Value="NearestNeighbor" />
        <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Input:VectorTextBox}">
                    <Border
                        x:Name="border"
                        HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="2"
                        SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="5" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Rectangle Grid.Row="0" Fill="{TemplateBinding LocateLineBrush}" />
                            <Container:VectorScrollViewer
                                x:Name="PART_ContentHost"
                                Grid.Row="1"
                                Panel.ZIndex="1"
                                Focusable="false"
                                HorizontalScrollBarVisibility="Auto"
                                IsAllowDragToScroll="False"
                                Style="{DynamicResource VectorScrollViewerStyle}"
                                VerticalScrollBarVisibility="Auto" />
                            <TextBlock
                                x:Name="waterMarker"
                                Grid.Row="1"
                                Margin="{TemplateBinding WaterMarkerMargin}"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Panel.ZIndex="0"
                                Background="Transparent"
                                Foreground="{Binding WaterMarkerBrush, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Input:VectorTextBox}}"
                                Text="{Binding WaterMarkerText, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Input:VectorTextBox}}"
                                TextAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasText" Value="False">
                            <Setter TargetName="waterMarker" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="HasText" Value="True">
                            <Setter TargetName="waterMarker" Property="Visibility" Value="Hidden" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>