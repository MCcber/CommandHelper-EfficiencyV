<UserControl
    x:Class="CBHK.View.Common.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:CustomControl="clr-namespace:CBHK.CustomControl"
    xmlns:VectorButton="clr-namespace:CBHK.CustomControl.VectorButton"
    xmlns:VectorCheckBox="clr-namespace:CBHK.CustomControl.VectorCheckBox"
    xmlns:VectorComboBox="clr-namespace:CBHK.CustomControl.VectorComboBox"
    xmlns:behavior="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CBHK.CustomControl"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    FontFamily="{Binding SelectedFontFamily}"
    mc:Ignorable="d">
    <StackPanel Margin="10,5">
        <VectorComboBox:VectorTextComboBox
            Title="启动器可见性"
            Grid.Row="1"
            Grid.Column="1"
            Height="40"
            Margin="0,0,35,5"
            HorizontalAlignment="Stretch"
            ArrowMargin="0 0 10 0"
            ItemsSource="{Binding StateList}"
            SelectedItem="{Binding SelectVisibleItem}"
            Style="{DynamicResource VectorTextComboBoxStyle}">
            <behavior:Interaction.Triggers>
                <behavior:EventTrigger EventName="SelectionChanged">
                    <behavior:CallMethodAction MethodName="ViewState_SelectionChanged" TargetObject="{Binding}" />
                </behavior:EventTrigger>
            </behavior:Interaction.Triggers>
        </VectorComboBox:VectorTextComboBox>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <VectorComboBox:VectorTextComboBox
                Title="字体"
                Grid.Column="0"
                Height="40"
                Margin="0,0,0,5"
                VerticalAlignment="Center"
                ArrowMargin="0 0 10 0"
                Foreground="White"
                ItemsSource="{Binding CurrentFontFamilyNameList}"
                SelectedIndex="{Binding SelectFontIndex}"
                SelectedItem="{Binding SelectedFontFamilyItem}"
                Style="{DynamicResource VectorTextComboBoxStyle}">
                <behavior:Interaction.Triggers>
                    <behavior:EventTrigger EventName="SelectionChanged">
                        <behavior:CallMethodAction MethodName="FontComboBox_SelectionChanged" TargetObject="{Binding}" />
                    </behavior:EventTrigger>
                </behavior:Interaction.Triggers>
            </VectorComboBox:VectorTextComboBox>
            <VectorButton:VectorTextButton
                Grid.Column="2"
                Width="35"
                Background="#D0D1D4"
                Style="{StaticResource VectorTextButtonStyle}"
                Text="..." />
        </Grid>
        <VectorComboBox:VectorTextComboBox
            Title="主题颜色"
            Grid.Row="2"
            Grid.Column="1"
            Height="40"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            ArrowMargin="0 0 10 0"
            Background="#D0D1D4"
            Foreground="White"
            Style="{DynamicResource VectorTextComboBoxStyle}" />
        <VectorComboBox:VectorTextComboBox
            Title="test"
            Grid.Row="1"
            Grid.Column="1"
            Height="40"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            ArrowMargin="0 0 10 0"
            Foreground="White"
            Style="{DynamicResource VectorTextComboBoxStyle}" />
    </StackPanel>
</UserControl>
